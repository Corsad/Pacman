#ifndef CHECK_WALL
#define CHECK_WALL
/** Checking whether the current location is a wall or not
  * Return 1 if it's not wall and return -1 if it's wall
  * @param n the char array
  * @param row the row that need to check
  * @param col the column that need to check
  */
int checkWall(char** n, int row, int col);

#endif