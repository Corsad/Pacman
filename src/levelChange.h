#ifndef Level_Change
#define Level_Change

#include <string.h>
#include <stdlib.h>
#include <stdio.h>

/** A function to combine 2 string into 1
  * @param s1 the string number 1
  * @param s2 the string number 2
  */
char* concat(char *s1, char *s2);

#endif

